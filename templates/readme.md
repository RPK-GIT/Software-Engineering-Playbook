# Project Name

<!--
README template - satisfies DOC-001: a new engineer can build, test, and run the project from this
file alone. Delete comments; delete sections that genuinely don't apply rather than leaving stubs.
-->

One paragraph: what this system does, for whom, and its profile (e.g., web + api-service + uses-database).

## Prerequisites

<!-- Exact tools and versions needed. Nothing "assumed installed". -->

## Build

<!-- The command(s). If it's more than three steps, script it and document the script. -->

## Test

<!-- How to run each test level that exists here (unit / integration / e2e). -->

## Run

<!-- How to run locally, including any local dependencies (and how to start them). -->

## Configuration

All configuration keys are enumerated and documented in the tracked template
(<!-- e.g., `.env.example` -->) per REPO-004 / DOC-005. Copy it and fill values for your
environment; values are never committed (REPO-003).

## Repository layout

<!-- Top-level directories and what lives in each (REPO-007). -->

## Engineering standards

This repository adopts the Software Engineering Playbook — version and profile are declared in
[CLAUDE.md](CLAUDE.md). Decisions live in `decisions/`.

## Troubleshooting

<!-- Known failure modes and fixes, added as discovered (DOC-008). Delete if empty at creation. -->
